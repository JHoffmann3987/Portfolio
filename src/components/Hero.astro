<section class="hero" id="hero">
  <p class="eyebrow">Hi, I'm Justin.</p>

  <h1>
    Hands-on infrastructure.<br />
    Real-world IT.<br />
    Continuous learning.
  </h1>

  <div class="description">
    <p>
      I'm an IT Support Technician working in a Managed Service Provider (MSP)
      environment, where I help businesses support and maintain their IT
      infrastructure.
    </p>

    <p>
      Outside of work, I build and document home lab projects to strengthen my
      skills with Microsoft 365, Linux, Proxmox, Docker, networking,
      virtualization, PowerShell, and automation.
    </p>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;

    display: flex;
    flex-direction: column;
    justify-content: center;

    max-width: 640px;

    padding: 2rem 0;
  }

  .eyebrow {
    color: #3b82f6;

    text-transform: uppercase;
    letter-spacing: 0.14em;

    font-size: 0.85rem;
    font-weight: 700;

    margin-bottom: 1.5rem;
  }

  .hero h1 {
    font-size: clamp(3.8rem, 7vw, 5.5rem);
    line-height: 0.9;
    letter-spacing: -0.04em;

    color: #f8fafc;

    margin-bottom: 2.5rem;
  }

  .description {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;

    max-width: 620px;

    color: #94a3b8;

    font-size: 1.1rem;
    line-height: 1.9;
  }

  .description p {
    margin: 0;
  }

  @media (max-width: 900px) {
    .hero {
      min-height: auto;
      padding: 3rem 0;
    }

    .hero h1 {
      font-size: clamp(3rem, 10vw, 4.5rem);
    }

    .description {
      font-size: 1rem;
    }
  }
</style>
